export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">Minimal starter</p>
        <h1>LAMP</h1>
        <p className="lede">A lean Vite + React + TypeScript skeleton.</p>
      </section>
    </main>
  );
}
